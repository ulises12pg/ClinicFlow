# ClinicFlow Backend — dependencias de producción
# Versiones flexibles para evitar conflictos de resolución.

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
starlette>=0.37.0,<0.38.0

# MongoDB
motor>=3.3.0
pymongo>=4.7.0
dnspython>=2.6.0

# Auth
PyJWT>=2.8.0
bcrypt>=4.1.0
passlib>=1.7.4

# Validation
pydantic[email]>=2.7.0
email-validator>=2.1.0

# Scheduler (recordatorios automáticos)
APScheduler>=3.10.0

# HTTP client (object storage fallback)
requests>=2.31.0
httpx>=0.27.0

# Email (recordatorios)
resend>=0.8.0

# Environment
python-dotenv>=1.0.0

# Crypto (python-jose dependency)
cryptography>=41.0.0

# Utilities
anyio>=4.3.0
